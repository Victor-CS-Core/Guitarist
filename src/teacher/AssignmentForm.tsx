import {useState} from 'react';
import {activities,skills} from '../curriculum/foundations';
import {useDemo} from '../demo/StoreProvider';
export function AssignmentForm({studentId}:{studentId:string}){const {state,dispatch}=useDemo(),student=state.students.find(s=>s.id===studentId)!;const options=activities.filter(a=>student.unlockedLevels.includes(skills.find(s=>s.id===a.skillId)!.levelId));const [activityId,setActivity]=useState(options[0]?.id??''),[minutes,setMinutes]=useState(3),[repetitions,setRepetitions]=useState(3),[message,setMessage]=useState('');return <form className="card form-card" onSubmit={e=>{e.preventDefault();const r=dispatch({type:'assign',studentId,activityId,minutes,repetitions,at:new Date().toISOString()});setMessage(r.ok?'Practice assigned. Your student can find it on Home.':r.error);}}><h2>Plan the next small step.</h2><p>Choose a focused activity from an unlocked chapter.</p><label>Practice activity<select value={activityId} onChange={e=>setActivity(e.target.value)}>{options.map(a=><option value={a.id} key={a.id}>{a.title}</option>)}</select></label><div className="form-row"><label>Target minutes<input type="number" min="1" max="60" value={minutes} onChange={e=>setMinutes(Number(e.target.value))}/></label><label>Rounds<input type="number" min="1" max="100" value={repetitions} onChange={e=>setRepetitions(Number(e.target.value))}/></label></div><button className="button" type="submit">Assign practice</button><p role="status" className="form-message">{message}</p></form>;}
